build/
out/
*.spv
vcpkg_installed/
compile_commands.json
vcpkg/

# Local vulkaninfo dumps (device/driver debug output, machine-specific)
VP_VULKANINFO_*.json

# F12-triggered manual screenshots (see Application::nextScreenshotPath)
/screenshots/

# Offline Python mesh-authoring tools
__pycache__/

# Local working notes: measurements, previews, and raw data from
# terrain/rendering experiments. Not committed — see PLAN.md's
# "Reference material" section for the load-bearing conclusions.
/docs/
